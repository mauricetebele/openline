export const dynamic = 'force-dynamic'

import AppShell from '@/components/AppShell'
import AmazonRefundsManager from '@/components/AmazonRefundsManager'

export default function AmazonRefundsPage() {
  return (
    <AppShell>
      <AmazonRefundsManager />
    </AppShell>
  )
}
